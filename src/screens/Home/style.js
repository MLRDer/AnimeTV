import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    searchBar: {
        elevation: 0,
        flex: 1,
        borderWidth: 0,
        padding: 0,
    },
    container: {
        width: '100%',
        marginTop: -8,
        marginBottom: 40,
    },
});
